streamlit>=1.37.0
pymysql>=1.1.1
python-docx>=1.1.2
openpyxl>=3.1.5
xlrd>=2.0.1
pdfplumber>=0.11.4
pypdf>=5.0.0
PyMuPDF>=1.24.10
Pillow>=10.4.0
pywin32>=306; platform_system == "Windows"

# Optional OCR backend. Install when PDF image pages must be recognized:
# rapidocr_onnxruntime>=1.3.24

# Retrieval dependencies used by modules 2 and 3:
sentence-transformers>=5.1.0
# Keep the runtime aligned with the FAISS index producer's verified version.
faiss-cpu==1.11.0
